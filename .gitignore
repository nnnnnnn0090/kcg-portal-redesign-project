# OS
.DS_Store

# Extension build output
/app/.output/
/app/.wxt/
/build/

# Dependencies
/app/node_modules/

# Environment (never commit secrets)
/app/.env
/app/.env.local
/app/.env.*.local

# TypeScript incremental build info
*.tsbuildinfo

# Unit / integration test output
/app/coverage/

# Playwright E2E (reports, cache, traces)
/app/test-results/
/app/playwright-report/
/app/blob-report/
/app/playwright/.cache/

# E2E Microsoft login session + Chrome profile (credentials on disk)
/app/e2e/.auth/

# Local browser-saved reference pages (not part of the product)
KCG WebMail*
kcg.edu*
/aaa_files
aaa.html
